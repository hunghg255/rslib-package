# Rslib Project

https://github.com/web-infra-dev/rslib

## Setup

Install the dependencies:

```bash
pnpm install
```

## Get Started

Build the library:

```bash
pnpm build
```

Build the library in watch mode:

```bash
pnpm build --watch
```
